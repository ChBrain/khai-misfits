---
---

Audits every hand-written baseline in the house. **The numbers were clean; the paths were not.**

**Three numeric baselines, two of which cannot go slack.** `UNDECLARED_BASELINE` and `UNINDEXED_BASELINE` both stand at zero, which is the tight end. `UNAXISED_BASELINE` was the only one with room, and the last pass capped it. The shape the last pass found was the exception rather than the rule.

**Thirty-two canon exemptions and a hundred and one homonym declarations, all live.** Every canon entry exempts a work carried by two or more misfits; every homonym resolves two or more index keys. Both lists earn their length.

**And the first canon audit was wrong, which is the method finding.** Matching the canon strings by hand reported _Political Parties: the iron law of oligarchy_ as dead. It is cited by three misfits. Run through `canonFamilies` — the view the wall itself reads — all thirty-two are live. **A hand-rolled matcher answers a question the wall never asked.**

**Fourteen path entries match nothing, and thirteen of them are correct.** Thirteen are `packages/**` allows for the workspace layout the fifty-eighth pass reasoned through and planned. The fourteenth is `governance/* :: REFERENCE.md` — singular, sitting beside the real `REFERENCES.md`, never existed, not part of the planned shape. A typo, removed.

**Cargo and a forward-declaration are indistinguishable from the config.** Both match nothing; only the register tells them apart. **An entry whose justification lives nowhere will be re-audited forever**, so the thirteen now carry their reason in Keep Clean.

And the house's rule _a lane allow for a path that cannot occur is cargo_ was written about a path nobody had argued for. **It does not reach paths somebody has.**

Ships nothing.
