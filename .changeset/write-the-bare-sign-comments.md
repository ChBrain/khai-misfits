---
"@chbrain/khai-misfits": patch
---

Work the bare signs on the axes that had some members commented and some not, which is where the comment is nearly free and also a test of the join. **Sixteen comments attempted, fifteen written, one refused and its play's sign withdrawn.** The bare count falls from 98 to 82 and the wall from 200 units declaring an axis to 199.

**Five or a Layer gives up `hierarchy-depth`, and its own first sentence is the reason.** It stages the organisation chart as "one dial with a cost written at each end": widen the span and each person gets a slice of a week already committed, narrow it and a layer is inserted that exists precisely so that less reaches the person above. **Attention and fidelity are two goods moving in opposite directions on one dial**, so there is no single quantity to say a sign about. The comment could not be written in one clause, which is exactly the test the last pass proposed, and it found this on its first outing.

**The dial survives and The Frozen Middle holds it alone.** That play's harm runs one way, because its layer is measured on continuity while charged with change, so it damps the large signal for a reason of its own rather than as an arithmetic remainder, and what it loses is a single quantity: how much of what was sent survives the rungs between the ends. Its joining paragraph is corrected accordingly, since it previously named Five or a Layer as an axis-mate.

The fifteen comments that wrote themselves are on `buffer-slack`, `codification`, `incentive-intensity`, `intervention-coverage`, `noise-share-of-the-measure`, `precommitment`, `protective-cover`, `record-granularity`, `response-lag` and `reviewer-proximity`. The reading also settled what a sign comment is for: it names **that play's** good quantity and not the axis's, which is why an opposed pair carries two comments about two different goods and is still one dial.
