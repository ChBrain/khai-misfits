---
---

Add `--undeclared-namesakes`, the namesake probe that runs before a declaration
rather than after one. Every existing namesake flag reads the index for a
surname already declared, so the only way to find one that ought to be declared
was to declare it speculatively. The first run found four genuine namesakes,
one scholar written two ways, and two cells where the computation did not find
a person at all.
