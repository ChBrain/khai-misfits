---
"@chbrain/khai-misfits": patch
---

Run `--families`, the selector the contract recommends for finding opposed pairs, and record why its yield is lower than its rationale. Of the thirteen family members with no axis, one declares honestly: **The Winning Bid** joins `noise-share-of-the-measure` as a third member, negative. Two were proposed by the selector and refused by their own warrants in their own words: The Dollar Auction is not `sunk-cost`, because each bid is forward-looking and the all-pay rule rather than what was spent makes the next bid worth its cost; The High Water Mark is not `baseline-recency`, because the floor is a real commitment that is defended rather than a level that has come to look ordinary.

The reason is that a shared canon work means a shared conversation, and a conversation is where an author staging a second play had to justify it, so distinctness paragraphs cluster inside families. `--families` is a high-yield selector for finding an argument already written and a low-yield one for finding a new axis member. Retires the axis-coverage slate line, whose instruction was to declare what `--families` ranks, and replaces it with selection by the dial. Strikes the apparent-competition line, which was a prohibition written as a task and can never be completed.
