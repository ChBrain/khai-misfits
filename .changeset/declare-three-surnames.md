---
---

Declare `Wilson`, `Daniels` and `List` in `scholarPolicy.homonyms`. Declaring
them found a third `Diamond` the probe could not see, sitting in a bare cell, so
`Diamond` is held back until its cell is named: an undeclared surname with a
bare cell owes the cell first.
