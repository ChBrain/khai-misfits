---
---

Declare the eleven surnames the mixed-cell read named, and add `Deirdre` under
`Wilson` ahead of the misfit that will cite her. Eighty declared surnames, zero
unresolved and zero unreachable forms, and `--undeclared-namesakes` now reports
nothing. `Ross` is the first declaration to separate two people who share a
given name, which only the forms ordering can do.
