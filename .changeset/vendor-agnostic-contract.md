---
---

Move the coding contract from CLAUDE.md, one vendor's file, to AGENTS.md, vendor
agnostic and applying in full to every agent that works here, named or not. The
house's own content moves verbatim: branching, authoring with every subsection,
versioning and protection are unchanged prose, only their address changes.

CLAUDE.md, GEMINI.md and .github/copilot-instructions.md thin to that tool's own
quirks and a pointer back to AGENTS.md. PERPLEXITY.md arrives the same way, an
address kept ready for a quirk that has not shown up yet. README.md carries the
one pointer that does not depend on a tool discovering a file by name.

management/management_instructions.md and management/position_playwright.md are
re-taken from the khai-stage blueprint, byte for byte: this house's management
core was never meant to diverge from the company every khai house shares.

Ships nothing.
