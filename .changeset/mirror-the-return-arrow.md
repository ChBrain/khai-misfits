---
"@chbrain/khai-misfits": patch
---

Repair **The Mirror**, which staged Conway's Law in one direction only. Every element in its encoding ran communication structure to design structure, and nothing held the return arrow: once the architecture exists it constrains the communication that can happen next, so the boundary drawn where two teams did not talk becomes the reason that silence continues, and the seam is load-bearing in both structures at once.

Adds one piece, **Now It Is the Reason**, since one load-bearing vector is one element, and wires it into the play, the reorg plot and The Reorg, whose own text presupposed the vector without staging it. Corrects the MacCormack, Rusnak and Baldwin row, which cited a paper titled for the duality and described only one arm of it, and adds Baldwin and Clark's Design Rules for the return arrow proper. Extends No Hero, because the reorganiser is not moving a boundary onto an empty field but arguing with a contract, a queue, an on-call rota and three years of code written against the split.

Also slates the reference group that cannot exist, the homophily cut of diffusion of innovations, at P1 with its switch-offs against After You and Information Cascade, and the `Rogers` widening that precedes it.
