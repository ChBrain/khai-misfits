---
---

Runs the joiner selector and declares nothing. **The null is the finding: the narrow phrase predicts a decision and not a join.**

Ninety-three of the 139 undeclared plays name a declared neighbour on a dial with two or more members, which is where every opposition this house holds has come from. The narrow phrase cut that to eight. **None of the eight belongs on the dial its sentence named.**

**The reason is structural.** The sentence the phrase matches is a _distinctness_ sentence, and a warrant that has properly cut itself has already reasoned about the quantity, so the row arrives **settled**. Settled rows go either way. Three of the eight were rows this register had already refused.

**And the phrase must sit in the same sentence as the neighbour, or it matches ordinary English.** The unconstrained grep returned five rows and every one was the phrase doing other work — _runs against the grain of how argument usually goes_, _the restraint runs against the drive that imposed the grid_. This is the shared-word rule arriving inside the narrow phrase that was supposed to be immune to it.

**The cheapest pre-filter found: the incumbent axis's own refusal clause.** Two rows named a member of `outcome-selection`, and both candidates state that axis's refusal clause in the very sentence that shortlisted them — _every subject is present and counted, nobody is absent from anything_, and _the absent are counted, and counted as though still present_. **When a candidate's own warrant states the incumbent axis's refusal clause, the row is decided before it is opened**, and an axis whose refusal clause is written down can be run as a grep rather than as a reading.

The other three refuse on the first monotonicity question, each in one stipulation: make every fix fully curative and First Do Harm runs entire; write everything down from the first day and Collingridge is untouched; set the reward on the measure to zero and the attenuation bound holds.

All three refinements carried to Find the Next One, with the sharper distinctness sentence the last pass could not ship from a misfit lane.

Ships nothing.
