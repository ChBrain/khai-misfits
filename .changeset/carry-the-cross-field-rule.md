---
---

Carries the cross-field dedup rule into Find the Next One, and runs it once so the plan is not holding a one-observation rule beside the measured ones.

**Run one**: grepping the whole warrant for sampling vocabulary across the hundred and two undeclared plays filed outside measurement returns eleven, six of them the word in another sense entirely — censor as censorship, attrition as bidders dropping out, samples as goods.

**Run two** refutes the obvious narrowing. Grepping the `## Arc` bend instead returns four, drops three noise hits, and loses the one real candidate: a play states its bend in its own field's words and names the borrowed discipline only where it names the phenomenon.

**Run three** finds the index. The `concept:` line returns five, keeps the candidate, and drops the market and bidding noise, because that line is where a play says what it is in the canonical terms of whatever literature owns it.

And the instrument that finds a cross-field dial is the instrument that finds a cross-field pun: four of the five hits were one word doing different work in two disciplines — selection Darwinian here and about samples there, bias cognitive and statistical, artefact a designed object and a measurement error. This is the namesake wall in another register, a discipline's word collating two meanings where a surname collates two people.

The refusals sharpened `outcome-selection` more than the hit did. Four neighbours refuse on **nothing is missing** — Safe to Say falsifies every value, Better in Both reclassifies every unit, True of No One aggregates a complete sample validly, the Spotlight Effect has no sample at all — and a fifth on a clause the axis had never had to state, that **it needs a reader** who takes the sample for the population.

Also corrects the lane note: Fill the Season rides the lane of the change it drives and is the only file under `management/**` that does, so a pass that declares an axis and amends a plan spans two lanes.

Ships nothing.
