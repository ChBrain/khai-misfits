---
---

Carry three passes of method out of the register and into the plans that hold it, which is what the register keeps concluding and nothing has yet acted on.

**Find the Next One** gains the instruments for the surname scan it already orders: `science probe` reads the whole house's namesake bill in one line, its second and longer list is a readiness report rather than a debt, `science opposed` reads the axis oppositions, and none of them reports the failure that actually stops a pass, which is a declared surname cited without a matching form and which only the suite sees. It also gains a second mode for the empty-end selector, which is where that selector's best result came from: the axis list is a duplication check and not a namespace, so before declaring a new axis, ask whether the quantity is already measured under a name the new play would never have guessed. And it gains the rule that a non-person Origin `Source` is a wording fault before it is a policy question, because declaring one in `scholarPolicy.nonAuthorSources` splits an authoring pass across two lanes for a formatting error.

**Name It Once** gains the two patterns that have actually broken its first rule: a plot named for the piece that cues it, and a position and a process on the same word. The canon catches both, so nothing ships broken; what they cost is a rename late in an authoring pass. Ships nothing.
