---
---

Widen `Katz` to `["Daniel", "Joel", "Michael", "Ralph", "Elihu"]`, clearing the declaration that stands in front of the reference group play. Elihu Katz is the Katz of _Medical Innovation_, the physician prescribing study the diffusion warrant cites for the same shape outside farming, and `Katz` is already declared, so the wall fires on the citation rather than waiting to be slated. A widening may land before its citation, because a declared form matching no cell is invisible to every wall.

Records why the scan before the authoring missed it: the scan looked up the surnames it expected to be namesakes and found the two that collate, and a **declared** surname is the opposite obligation to a taken one. A surname the house already holds is a cell to read and owes nothing; a surname the house has declared is a wall. Run the suite before deciding what a play owes, since it reports the whole bill in one line. Ships nothing.
