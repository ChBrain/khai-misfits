---
---

Scout the replacement candidate the last pass slated, a play with stereotype threat's shape on a sounder evidential base, and find one: **the nocebo bind in informed consent**. A prescriber is obliged to name the side effects, the naming produces them, and the duty and the harm are the same act. The evidence is unusually strong for this kind of claim: an n-of-1 crossover trial found ninety per cent of the symptom burden on a statin was also elicited by placebo, and symptoms were worse on placebo than on no tablet at all, which is a within-patient demonstration rather than a between-group inference. The bind itself is named and argued in the bioethics literature rather than being the house's own reading. Not in the house: no nocebo anywhere, and the nearest neighbours, the surgical placebo problem, the Tarasoff disclosure chill, the observer's paradox and the patch that specifies the flaw, are each a different engine.

**The play is not authored here, because it carries three namesake obligations and one of them fires a wall.** Citing the trial needs `Howard` and `Francis`, both bare and both a different person from the one in the house; citing the consent argument needs `Wells`, which is **declared** on two forms, so the citation would fire on arrival. Clears the first obstacle in this lane: the diagnostic-overshadowing row in Already Explained now names **Louise Howard** and **Graham Thornicroft**.

**And clearing it turned up a property of the instrument worth more than the obstacle.** `science surname` prints `(bare)` for a surname that is **undeclared**, not for one whose cells fail to name a person. Naming both authors in that row left it reading `(bare)`, and it will keep reading `(bare)` until `Howard` is declared. Every resolved key in the house belongs to a declared surname, and `Murphy` read `(bare)` for as long as it did while both of its cells named their person in full. **The label says unnamed and means undeclared**, which misled this pass twice before the pattern held. Ships nothing.
