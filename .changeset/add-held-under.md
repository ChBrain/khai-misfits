---
"@chbrain/khai-misfits": minor
---

Add the misfit Held Under: a transient trigger tips a sound system into a failing state, and its clients' own correct retries become the load that holds it there after the trigger is gone, so the failure sustains itself and clears only when the loop is broken by force, at the cost of the real work shed.
