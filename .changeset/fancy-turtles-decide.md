---
---

Close the coverage hole in the opposed-pair gate. The axis is read from each misfit's REFERENCE.md frontmatter instead of khai-guard.config.json, so it rides the misfit's own lane, and coverage is ratcheted at 238 so a new misfit that declares no axis fails rather than going unchecked.
