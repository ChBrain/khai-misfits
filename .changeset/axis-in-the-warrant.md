---
"@chbrain/khai-misfits": patch
---

Move the eight axis and sign declarations from khai-guard.config.json into each misfit's own REFERENCE.md frontmatter, so a misfit declares the quantity it acts on beside the warrant that argues it, and so a new misfit can declare its axis in the pull request that adds it.
