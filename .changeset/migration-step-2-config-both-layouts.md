---
---

Migration step 2: the lane config learns the workspace, as an addition rather than a replacement, so it is correct before and after the move and imposes no freeze. The misfit lane names the house rather than taking `packages/**`, which would grant it a cross-lane pass over the package's README and concordance. A new wall holds the config to both layouts. Ships nothing.
