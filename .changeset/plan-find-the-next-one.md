---
---

Add `management/plan_find_the_next_one.md`, collecting the six selectors the house has worked out for finding the misfit it does not yet hold, each with what it searches, what it has returned, and how it fails, together with the standing rules that govern all of them.

The reason is a number: the register is over a megabyte, every finding in it is discoverable only by grep, and the passes have been re-deriving their own rules. It is a `plan_*.md` rather than a new kind of file, because the management README says the directory arrives from the blueprint and `plan_*.md` is already the documented category for recurring procedures the house runs, which Name It Once already uses for craft rules of the same kind. What went into it is the measured yield rather than the method, since the method is easy to think of and the yield is what cost passes to learn. Ships nothing.
