---
---

Add `--mixed-cells`, the complement of the undeclared-namesake probe, and
declare the six surnames the last pass named. The probe needs two named cells to
see a surname, so a namesake whose counterpart is bare is invisible to it; the
complement reports where that would be hiding, prints its own total, and says
that the total is a reading list rather than a finding count.
