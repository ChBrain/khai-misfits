---
---

Audits the slate's deferrals against the walls, finds one, and finds something worse beside it.

**Nineteen open lines, and exactly one deferred what a wall demands** — the axis declaration the ratchet caught last pass. The rest are questions, reading tasks and process notes that no wall requires, so the mistake was a single case rather than a pattern.

**What the audit found instead is that a number in a slate line is a hand-written baseline.** Five open lines carry one and **three are stale**: _183 of 336_ against 207 of 345, _144 uncarried_ against 115, _a hundred and seven remain_ against 115. Two passes ago this house audited every hand-written baseline it holds and **scoped that audit to `tests/` and the config. The slate holds more of them than the code does.**

**And one of the three is stale upward.** The uncarried count went 144 to 107 across four reading stretches and is now 115, because every pass writes some. **A backlog with an inflow every pass and an outflow only on stretch passes does not converge by being worked at intervals** — the standing order holds the inflow, the stretches clear the stock, and they are different jobs. Carried to Keep Clean.

Three leftovers carried, all things a misfit lane could not ship: the non-author Source rule to Cut to Fit; the naming rule to Name It Once, with the measurement that earned it (an authoring pass naming against the three known collisions produced thirty-six files and **zero** title collisions, against three on the pass before); and the register-rules filter now holds the house's own capitalised column names, fixing its first false negative.

**And the instrument found a third gap in itself on this entry** — its work-frame test knew `a pass` and not `every pass`. Three gaps have now been found by running it on the pass writing about it, which is the only place the missed rule is still in front of you.

Ships nothing.
