---
"@chbrain/khai-misfits": patch
---

Run a synonym audit over the 117 axes carrying a single play, and merge one pair that two names had made invisible. **Whoever Is Not Involved** declared the approver's `independence` and **Near Enough to Doubt It** declared the reviewer's `proximity`, one dial read from opposite ends, and both declared it negative, so the house held two plays making opposite claims about one quantity while each agreed with nobody. Merged onto `reviewer-proximity` the opposition fires, and what sets the sign is whether the reviewer is asked for a verdict or for a rank.
