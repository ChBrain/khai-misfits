---
---

Declare `Edwards` ahead of the misfit that needs it, and record the Channel That
Closes gate as cleared. The house holds A W F Edwards of the sex-ratio tracing
in a cell that already names him, which is the ordering table's middle row: no
cells are owed and the declaration goes first, so `Griffin` is declared against
no cell exactly as `Deirdre` was.
