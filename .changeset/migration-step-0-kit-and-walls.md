---
---

Migration step 0. Take khai-tests 0.3.4, whose policy loaders walk up from a
content root to find the guard config, and prove it against this house's own
config from a simulated nested root: 32 canon, 3 supporting markers and 81
declared surnames all resolve. Add the packing wall before the move rather than
after it, and swap the changeset wall onto the kit's workspace reader, which is
required for correctness once a private root exists.
