---
---

Refill the slate with a new selector: read the empty end of every singleton dial. The house has 174 declared axes, 113 carrying a single play, and each is a question about what happens at the other end. Two recent finds were exactly this and were not recognised as a method at the time.

The main result corrects what the ratchet's shape means: most singletons have one bad end only, so they can never carry an opposed pair and will stay singletons however long the ratchet runs. The coverage number counts plays that have declared, not oppositions waiting to be found. The second failure mode is a dial harmful at both ends, which the contract already says has no sign, so an empty end is a candidate only where the two ends have different engines rather than merely different signs.

Slates the silent patch at P2 with that caution attached, and records that the shared-work wall constrains it in advance: the sibling play already holds the patch-diffing canon, so a second play in that literature must be built on the disclosure-practice side and state the weaker sourcing in Restrictions. Ships nothing.
