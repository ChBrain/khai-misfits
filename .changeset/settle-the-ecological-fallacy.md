---
---

Settle the caution on the ecological fallacy. Both switch-offs pass, which this register already recorded, so on the written rule it is a play; the unease was that it would be staged in the room Wherever You Draw It already occupies. The settlement is therefore a staging decision: set it on the secret ballot and racially polarised voting, where the individual data is impossible in principle rather than merely unpublished, which is a different play in every seat. The general rule: where two plays pass each other's switch-offs but share a room, the separation must be found in the staging, and a candidate that can only be staged in the incumbent's room is a row whatever the logic says. The pre-authoring scan found two further bare namesakes, `Goodman` and `Freedman`, the latter being the closest pair the house has met since both are David. Widens the `Robinson` declaration to a third form, having tested that an unmatched declared form is invisible to every wall, which is the opposite of what the last pass asserted. Ships nothing.
